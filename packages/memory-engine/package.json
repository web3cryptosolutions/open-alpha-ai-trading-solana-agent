{
  "name": "@openalpha/memory-engine",
  "version": "0.1.0",
  "description": "Local-first persistence + recall. Remembers every trade, recalls similar setups, learns patterns.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@openalpha/types": "workspace:*"
  }
}
