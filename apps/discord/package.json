{
  "name": "@openalpha/discord",
  "version": "0.1.0",
  "private": true,
  "description": "Discord control surface for the Open Alpha agent. (command layer ready; wire discord.js for the gateway)",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/bot.ts",
    "start": "tsx src/bot.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@openalpha/api": "workspace:*"
  }
}
